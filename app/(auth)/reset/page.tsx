import { Suspense } from 'react'

import ResetForm from '@/components/auth/reset-form'

const Reset = () => {
  return (
    <Suspense>
      <ResetForm />
    </Suspense>
  )
}

export default Reset
